apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-custom-config-vars
  namespace: default
data:
  EXAMPLE1_DISPLAY_NAME: "Example One (minikube)"
  # For subsequent runs after the first startup, uncomment the following line.
  # This will make the setup scripts use the permanent KEYCLOAK_CONFIG_CLI_CLIENT_ID client (e.g. "keycloak-config-cli").
#  KEYCLOAK_CLIENTID: "keycloak-config-cli"
